import { Button } from 'frog-components';


export const metadata = {
  title: 'Button',
  description:
    'Button component.',
}

# Button

Button component.

### Properties

<Properties>
  <Property name="children" type="string">
    The text of the button to be rendered.
  </Property>
  <Property name="variant?" type="'primary' | 'strong' | 'borderless'">
    The stylistic variant of the button.
  </Property>
  <Property name="icon?" type="React.ReactNode">
    Icon to be displayed on the button.
  </Property>
</Properties>

---

## Variants

<Row>
  <Col fullWidth>

    <CodeGroup>
        ```js
        <Button>Primary</Button>
        <Button variant="strong">Strong</Button>
        <Button variant="borderless">Borderless</Button>
        ```
    </CodeGroup>

    <ComponentDisplay>
        <div className="flex flex-row space-x-2">
        <Button variant="primary">Primary</Button>
        <Button variant="strong">Strong</Button>
        <Button variant="borderless">Borderless</Button>
        </div>
    </ComponentDisplay>

  </Col>
</Row>
